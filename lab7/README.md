Functions allow for less repeated code, so more reusable code that is cleaner than before.

The DateTime function serves a similar purpose to the other functions, it simplifies adding date, along with adding the ability to do date calculations.

DRY in code ensures you only use whats needed, which like the previous points makes cleaner code.