<?php query("SELECT * FROM `books` ORDER BY `title`"); 
// Check if we have books 
// if ($stmt->rowCount() > 0) { echo " Found " . $stmt->rowCount() . " while ($book = $stmt->fetch(PDO::FETCH_ASSOC)) { echo ""; echo "?>